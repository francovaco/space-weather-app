// src/app/(dashboard)/layout.tsx
// Groups all section pages under the dashboard layout
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
